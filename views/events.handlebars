<div class="event-container">
    {{!-- upcomming events --}}
    <div>
        <h3 class="event_headline">Upcoming Events</h3>
        {{#each events as |event|}}
            {{#if is_upcoming}}
                {{> eventCard}}
            {{/if}}
        {{/each}}
    </div>

    {{!-- previous events --}}
    <div>
        <h3 class="event_headline">Previous Events</h3>
        {{#each events as |event|}}
            {{#if is_upcoming}}
            {{else}}
                {{> eventCard}}
            {{/if}}
        {{/each}}
    </div>
</div>